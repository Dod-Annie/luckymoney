server:
  port: 8081
  servlet:
    context-path: /luckymoney

limit:
  minMoney: 1
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/luckymoney?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: prisma
  jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
